[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[scripts]
build-docs = "sh -c 'sphinx-apidoc -f -o ./docs/source ./ && cd docs && make html'"

[dev-packages]
sphinx = "*"
sphinx-rtd-theme = "*"

[packages]

[requires]
python_version = "3.8"
